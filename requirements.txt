# Exact pinned versions used to produce the metrics and sample outputs
# documented in this repo's README (PR-AUC 0.98, ROC-AUC 0.999, etc.).
#
# requirements.txt uses floor-only version bounds (e.g. pandas>=2.0.0),
# which is convenient day-to-day but means a fresh `pip install` next month
# could silently pull a newer major version of scikit-learn or pandas —
# and library defaults changing under you can shift model behavior without
# any of your own code changing.
#
# If you want to reproduce the exact reported numbers, install from this
# file instead:
#   pip install -r requirements-lock.txt

pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
matplotlib==3.10.8
joblib==1.5.3
pytest==9.1.1
